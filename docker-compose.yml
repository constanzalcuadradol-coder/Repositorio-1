services:
  app-java:
    build: ./
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      POSTGRES_USER: postgres
      POSTGRES_PASS: postgres
    ports:
      - "8080:8080"
